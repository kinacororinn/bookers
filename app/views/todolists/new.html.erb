<h1>New Book</h1>
<!-- form_with部分 -->
<%= form_with model:@book,url: '/todolists',local:true do |f| %>

  <h4>title</h4>
  <%= f.text_field :title %>

  <h4>body</h4>
  <%= f.text_field :body %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit 'CreateBook' %>

<% end %>